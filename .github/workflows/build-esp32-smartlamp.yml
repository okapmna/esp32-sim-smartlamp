name: Build ESP32 .bin

on:
  push:
    branches: [ main, master ]
  pull_request:
    branches: [ main, master ]
  workflow_dispatch:

jobs:
  build:
    runs-on: ubuntu-latest
    steps:
      - name: Checkout Code
        uses: actions/checkout@v4

      - name: Install arduino-cli
        run: |
          curl -fsSL https://raw.githubusercontent.com/arduino/arduino-cli/master/install.sh | sh
          echo "${PWD}/bin" >> $GITHUB_PATH

      - name: Configure arduino-cli for ESP32
        run: |
          arduino-cli core update-index --additional-urls https://raw.githubusercontent.com/espressif/arduino-esp32/gh-pages/package_esp32_index.json
          arduino-cli core install esp32:esp32 --additional-urls https://raw.githubusercontent.com/espressif/arduino-esp32/gh-pages/package_esp32_index.json

      - name: Install Libraries
        run: |
          arduino-cli lib install "PubSubClient"
          arduino-cli lib install "ArduinoJson"

      - name: Compile Sketch
        run: |
          mkdir -p build
          mkdir -p main
          
          # Copy semua file .ino ke folder main
          cp *.ino main/
          
          # Cek secret.h, jika tidak ada gunakan example
          if [ -f secret.h ]; then
            cp secret.h main/
          else
            cp secret.h.example main/secret.h
          fi
          
          arduino-cli compile --fqbn esp32:esp32:esp32 --output-dir build main/

      - name: Upload Build Artifacts
        uses: actions/upload-artifact@v4
        with:
          name: serialread-esp32-bin
          path: build/*.bin

      - name: Send .bin to API
        env:
          OTA_API_KEY: ${{ secrets.OTA_API_KEY }}
        run: |
          BIN_FILE=$(ls build/*.bin | head -n 1)
          # Ambil versi dari main.ino secara otomatis
          VERSION=$(grep "current_version =" main.ino | cut -d '"' -f 2)
          
          curl -X POST https://otaup.okamna.my.id/api/upload \
               -F "firmware=@$BIN_FILE" \
               -F "version=$VERSION" \
               -F "device_type=esp32-smartlamp" \
               -H "x-api-key: $OTA_API_KEY"